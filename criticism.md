# Criticism & Review of d3-thematika

このドキュメントは、`d3-thematika` プロジェクトの現状（v0.0.1-alpha）におけるアーキテクチャ、構成、拡張性についての批評と改善提案をまとめたものです。

## 1. 全体的なアーキテクチャ

### 評価: 👍 良好
プロジェクトは明確なレイヤーベースアーキテクチャを採用しており、関心の分離が適切に行われています。
- **Mapクラス**: オーケストレーターとして機能し、DOM操作と全体のステート（投影法、サイズ）を管理。
- **LayerManager**: レイヤーのライフサイクル（追加、削除、並び替え）とレンダリング順序を一元管理。
- **BaseLayer**: 全レイヤーの共通基盤として機能し、拡張性を担保している。

### 懸念点 & 改善提案
- **状態管理の分散**: 投影法(`projection`)の情報が `Map` -> `LayerManager` -> `Layer` と伝播する仕組みだが、レイヤー側で個別に状態を持つため、同期ズレのリスクがある。
    - *提案*: レイヤー描画時に常にコンテキスト（投影法など）を引数として渡す「ステートレス」な描画メソッドへの移行を検討しても良いかもしれない（ただし現状のImmutable方針とはトレードオフ）。
- **依存関係の注入**: 現在 `LayerManager` は `Map` クラス内でインスタンス化されているが、これを外部から注入可能にすることでテスト容易性が向上する。

## 2. レイヤーシステムと拡張性

### 評価: 👍 柔軟性が高い
`BaseLayer` を継承することで新しいレイヤーを容易に作成できる構造になっています。
- **属性の柔軟性**: `attr` や `style` プロパティにおいて、固定値と関数（`(feature, index) => value`）の両方を受け入れる設計は、D3.jsユーザーにとって直感的で使いやすい。
- **スタイルの適用**: `applyAllStylesToElements` メソッドにより、共通のスタイル適用ロジックが再利用されている点は評価できる。

### 懸念点 & 改善提案
- **D3への強い結合**: カスタムレイヤーを作成するには `d3-selection` の知識が必須となる。ライブラリの目標が「D3のパワーを活用する」ことなら問題ないが、「D3を隠蔽して簡単に使える」ことを目指すなら、描画ロジックの抽象化レベルを上げる必要がある。
- **型安全性**: `applyAttributesToElements` メソッドなどで `any` キャストが見受けられる。ユーザー定義のプロパティ関数に対する型推論を強化することで、開発体験（DX）を向上できる。

## 3. 汎用性とImmutableパターン

### 評価: ⚖️ トレードオフのバランス
設定変更ではなく「インスタンス再生成」を推奨するImmutableな設計方針は、状態管理を単純化する上で有効である。

### 懸念点 & 改善提案
- **パフォーマンス**: 大規模なGeoJSONデータを持つレイヤーを頻繁に再生成する場合（例: UIスライダーでのインタラクション）、データのパースやメモリ割り当てのコストが懸念される。
    - *提案*: `setData` や `setAttr` のような、重い処理をスキップできる「部分更新」メソッドを特定のレイヤー（GeojsonLayer等）には例外的に導入することを検討しても良いかもしれない。あるいは、データ参照だけを保持する軽量な更新フローを確立する。

## 4. プロジェクト構成とディレクトリ

### 評価: 🚧 整理が必要
現在の `src/layers/` はフラットな構造だが、レイヤー数が増加（現在13種類以上）しているため、見通しが悪くなりつつある。

### 改善提案
以下のようなディレクトリ構造へのリファクタリングを推奨する：
```
src/
  layers/
    core/       (BaseLayer, Interfaces)
    geo/        (GeojsonLayer, GraticuleLayer)
    point/      (PointCircleLayer, PointSymbolLayer...)
    line/       (LineConnectionLayer...)
    raster/     (ImageLayer...)
```

## 5. 特定の実装詳細についての指摘

### `Map.ts`
- **`fitBounds`**: 投影法によっては `invert` メソッドを持たないものがある（一部の複雑な投影法）。現状の実装では `invert` がない場合のフォールバックが `[0, 0], [width, height]` となっているが、これが適切か再考の余地がある。
- **エクスポート機能**: `savePNG` はブラウザの `Canvas` を利用しているが、外部CSSやWebフォントの扱いなどで予期せぬ結果になることが多い。既知の制限事項としてドキュメント化するか、より堅牢なライブラリ（html2canvas等）の利用検討が必要かもしれない。

### `GeojsonLayer.ts` / `PointCircleLayer.ts`
- **クラス名の扱い**: `attr.className` と `d.properties.class` を結合しているが、CSSクラス名の衝突や管理がユーザー任せになりがち。プレフィックスの強制や、CSS Modules的なアプローチがあるとより安全。

## 6. 結論

`d3-thematika` は明確な目的と堅実なアーキテクチャを持っており、アルファ版としては十分な品質に達している。
特に「静的な主題図」にフォーカスすることで、複雑になりがちなマップライブラリの仕様をスリムに保っている点は大きな強みである。

今後の課題は、**「拡張性の維持」**と**「パフォーマンス（特に再生成時の）」**のバランス、そして**「型安全性の向上」**にある。
