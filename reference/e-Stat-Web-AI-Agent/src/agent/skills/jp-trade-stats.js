// 財務省貿易統計スキル（ブラウザ移植版）。
// CLI版 jp-trade-stats の SKILL.md / trade-tables.md から、テーブル選択を誤らせない
// 中核知識だけを、本アプリのツール名・フィルタキーへ翻案して凝縮したもの。
// 品目・国コードは埋め込まず get_stats_metadata で都度特定させる（トークン節約）。
export const JP_TRADE_STATS_SKILL = `# 貿易統計スキル（財務省貿易統計 / e-Stat）

日本の輸出入データ（HS品目別・国別・時系列・前年比・国別ランキング）の依頼では、財務省貿易統計（普通貿易統計, 政府統計コード statsCode=00350300）を e-Stat から取得して分析する。

## e-Stat APIの構造について

### 政府統計と統計表

e-Statは以下の階層構造になっている。

政府統計
  └─ 統計表（データセット）
       ├─ 統計表
       ├─ 統計表
       └─ ...

例：

財務省貿易統計（statsCode=00350300）
  ├─ 品別国別表（2025年確報）
  ├─ 品別国別表（2024年確報）
  ├─ 国別概況品別表
  └─ ...

1つの政府統計から多数の統計表が公開される。

分析対象は政府統計ではなく統計表（statsDataId）であるため、まず対象の統計表を特定する。

---

### 統計表の構造

統計表は次の2つで構成される。

統計表
 ├─ メタ情報（CLASS_INF）
 └─ 実データ（VALUE）

* CLASS_INF = 軸定義・コード体系
* VALUE = 実際の観測値

e-Statのコード体系は統計表ごとに異なるため、コードを推測してはならない。

必ず metadata を取得して確認する。

---

### VALUEとCLASS_INFの関係

e-Statのデータは、

* VALUE = 実データ
* CLASS_INF = コード辞書

の組み合わせで解釈する。

例：

{
  "VALUE": {
    "@area": "840",
    "@cat01": "090111000",
    "@time": "2024000000",
    "$": "123456789"
  }
}

このままでは意味は分からない。

CLASS_INFを参照して

840         → アメリカ
090111000   → 未焙煎コーヒー
2024000000  → 2024年

へ変換する必要がある。

つまり

VALUE      = ファクトデータ
CLASS_INF  = コードマスター

として扱う。

---

### e-Stat JSONの特徴

e-StatのJSONはXMLを機械的にJSONへ変換した形式である。

主なルール：

| JSON | 意味        |
| ---- | --------- |
| @属性名 | XML属性     |
| $    | 要素の値      |
| 配列   | 同名要素の繰り返し |

例：

{
  "CLASS": {
    "@code": "090111000",
    "@name": "未焙煎コーヒー"
  }
}

これは

code = 090111000
name = 未焙煎コーヒー

を意味する。

e-Stat APIを扱う際は、

* "@" = 属性
* "$" = 値

であることを理解しておく。

---

### 固定軸と可変軸

e-Statでは次元IDに一定の慣習がある。

| ID          | 一般的な意味 |
| ----------- | ------ |
| tab         | 表章項目   |
| area        | 地域・国   |
| time        | 時間     |
| cat01～cat15 | 分類軸    |

ただし cat01～cat15 の意味は統計表ごとに異なる。

例：

cat01 = 男女別

cat01 = 年齢階級

cat01 = HS品目

同じ cat01 でも全く別の軸になる。

次元IDだけで判断せず、metadata の内容を確認すること。

---

### 階層コード

多くの分類コードは階層構造を持つ。

例：

09         コーヒー・茶類
└─0901     コーヒー
  └─090111000 未焙煎コーヒー

metadataには通常、

* level
* parentCode
* addInf

が含まれる。

これらを利用すると大分類・中分類・小分類単位で集計できる。

---

### データ取得の原則

* コード体系は統計表ごとに確認する
* cat01〜cat15 の意味を推測しない
* metadata を取得してからデータ取得する
* VALUE単独では解釈できない
* 統計表が変わればコード体系も変わる可能性がある

e-Statは実質的に

* データ（VALUE）
* コード辞書（CLASS_INF）
* 多次元軸（time, area, cat01〜cat15）

から構成される多次元データキューブである。

分析時はまず metadata を取得し、軸定義とコード体系を確認してからデータを取得する。

---

## 手順

1. search_stats_tables で対象の統計表を探す（statsCode="00350300"、searchWord に「品別国別表 AND 輸入 AND 確報」などを指定）。
2. get_stats_metadata で次元とコードを必ず確認する（コードを推測してデータ取得しない）。
3. fetch_stats_data で計測を固定して取得する。
4. inspect_dataset で件数・列・単位を確認してから回答する。

---

## 効率（反復を浪費しない）

* get_stats_metadata は1回の呼び出しで全次元とサンプルコードを返す。
* 次元ごと・query文字列ごとに何度も呼ばない。
* 必要なコード（品目・cat02・time・area）は表ごとにまとめて読み取る。
* コードが確認できたら過剰な再確認をせず、速やかに fetch_stats_data → 回答へ進む。
* 複数の statsDataId から取得した保存済みデータを比較する場合は、execute_analysis_javascript の datasetIds を使って同時参照する。別データセットの全行を args に詰めようとしない。
* 反復には上限がある。回り道を避け、確認できた情報で取得・回答を完了させること。

---

## 統計表の選び方（最重要）

* タイトルの「輸出 / 輸入」「速報 / 確報 / 確々報 / 確定」「対象年」で選ぶ。
* 分析・時系列には確報以降を使う（速報は暫定値で後日改定される）。
* 貿易統計表は年・公表段階ごとに別の statsDataId で発行される。
* 検索結果のタイトルと surveyDate で、目的の年が含まれる表を選ぶこと。
* 2020〜2025年のような複数年比較・時系列では、複数年を含む確報の通期表を選ぶか、年ごとの表を個別に取得し、execute_analysis_javascript の datasetIds で同時参照する。
* 単年だけの表に複数年の期間を指定すると「該当データなし」になるため、その場合は表の対象年を確認し直す。

### 主な表タイプ

| 表タイプ      | 用途             |
| --------- | -------------- |
| 品別国別表     | 9桁HS品目 × 国 × 月 |
| 国別概況品別表   | 国別・品目別集計       |
| 概況品別統計品目表 | 品目別集計          |
| 貿易指数表     | 指数分析           |

国別比較では通常「品別国別表」を使用する。

---

## 品目・国コードの特定

* 品目（9桁HS）・国コードは推測しない。
* get_stats_metadata の query（名称部分一致）で特定する。

### HS章の例

| HS章 | 内容      |
| --- | ------- |
| 09  | コーヒー・茶類 |
| 18  | ココア     |
| 27  | 鉱物性燃料   |

ただし章コードは手掛かりに留め、実際の9桁コードは必ず metadata で確認する。

---

## 全国合計（国指定なし）の扱い

* 品別国別表の area（国）次元に「総数」「世界計」コードは存在しない。
* area単独で全国合計は取得できない。
* 「全国の輸入額」で国が未指定の場合は、品目と年計金額を固定し、areaフィルタを付けずに取得する。
* 全国値は全相手国の合計として扱う。
* 不要な国コード探索で反復を浪費しない。
* 可能なら対象国・期間を依頼者に確認し、取得範囲を絞る。

---

## 次元と計測の固定

### 公式APIの絞り込み条件

getStatsData の絞り込み条件は、事項ごとに同じ構造で指定する。

* 表章: tab
* 時間軸: time
* 地域: area
* 分類: cat01〜cat15

各事項には cd / cdFrom / cdTo / lv がある。

例：

* cdCat01
* cdArea
* cdTimeFrom / cdTimeTo
* lvCat01

cd系は単一コードまたはカンマ区切り最大100個まで指定できる。
同一事項で lv と cd、または cd と cdFrom/cdTo を併用すると AND 条件になる。
From のみは以降すべて、To のみは以前すべてを意味する。

### time

time は e-Stat 公式APIの時間軸である。
年次・月次などの粒度やコード体系は統計表ごとに metadata で確認する。

品別国別表では、年次コードとして使われることが多い。

例：

2024年 = 2024000000
2023年 = 2023000000

品別国別表では、月別の金額・数量情報が time ではなく cat02 側の計測項目に入る表がある。
ただし、必ず metadata の time と cat02 を確認してから判断する。

cdTime / cdTimeFrom / cdTimeTo は、metadata で確認した time のコード体系に合わせて指定する。

---

### cat02

品別国別表には独立した tab（表章項目）次元が無い。

金額・数量・月別情報はすべて cat02 に格納される。

例：

| cdCat02 | 内容        |
| ------- | --------- |
| 140     | 合計_金額（年計） |
| 170     | 1月_金額     |
| 200     | 2月_金額     |

比較や集計の前に、目的の計測値へ固定する。

例：

cdCat02 = "140"

固定しないと金額・数量・月別データが混在して集計結果が壊れる。

表によって意味が変わるため、実際の割り当ては metadata で確認する。

---

### 単位

* 金額は通常「千円」
* 数量は品目ごとに異なる

metadata または inspect_dataset で unit を確認すること。

---

## 出典

結果には出典として以下を明記する。

財務省貿易統計 / e-Stat
`;
